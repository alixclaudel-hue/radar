# Radar — instructions projet

Outil perso crate-digging vinyle sur Discogs : ingère écoute (YouTube/Spotify/Bandcamp/DJ sets), profile labels/artistes, note sorties selon goût. Cible multi-utilisateur (chantier en cours) → `docs/architecture.md`.

**Reprise de contexte : ce fichier suffit** — état, TODO, pièges ci-dessous.

**Historique complet + détails techniques** : `claude_archive.md` + `docs/archive/` (`.claudeignore`, non lus auto) — lire explicitement (`Read <fichier>`) si détail manquant. **Avant de lire un doc non listé ici** (nouveau fichier, `docs/archive/`, `claude_archive.md`) : demander à l'utilisateur si pertinent.

**Docs techniques scoring** (générés session cloud 11/09 depuis le code, pas de MAJ auto — relire si le code évolue) :
- `docs/app-overview-scoring-lot1.md` + diagramme (`docs/app-diagram-brief-scoring-lot1.md`, `docs/app-overview-scoring-lot1.excalidraw`) : vue d'ensemble `scoring.py` (`Ctx`), migration `labels`/`watchlist` → `label_categories`.
- `docs/app-overview-discogs-dump-py-and-scoring-process.md` : périmètre plus étroit, uniquement `discogs_dump.py` + `scoring.py`.

**Mode caveman par défaut** : skill `caveman` (niveau `full`) en début de session. Réponses conversationnelles seulement — code/commits/doc/tickets restent en prose normale, sauf demande explicite (ce fichier compressé sur demande utilisateur le 11/09).

**Vérif par défaut (session cloud)** : smoke test hors-ligne seulement, jamais en conditions réelles (pas de token/accès réseau/SSH VPS) — sauf mention explicite « vérifié VPS » / « conditions réelles » sur un point.

## Prochaine session — reprendre ici

**Session du 17/09 (cloud)** :
- **PR #162** (pt 55) : playlist RECOS — colonne « Track », repli vinyle par titre de PISTE, exclusion des albums déjà en collection. **Mergée et déployée** (`751d9ed`).
- **PR #163** (pt 56) : « Nouveautés » (/veille) ET « Vendeurs » mises en PAUSE via `radar_web/radar/features.py`. **Mergée et déployée** (`ead6964`).
- **PR #165** (pt 57) : perf `/univers?tab=cart` (~190s→instantané) + route `/wantlist` dédiée. **Mergée et déployée** (`8dae861`). Volet C (tri de la base de labels) du même brief **volontairement pas codé** : nécessite un arbitrage utilisateur (seuil, mécanique de retrait), cf. pt 57.
- **Piège rencontré en ouvrant la PR #165** : la branche `claude/hello-e87dpo` portait encore son propre commit `034be62` (docs refresh de la session précédente), déjà mergé sur `main` sous un autre sha (`683c8cf`, PR #164) — même contenu, hash différent (squash/re-commit côté GitHub). Un merge direct aurait fait un vrai conflit sur `CLAUDE.md` (deux insertions indépendantes du même bloc). Corrigé en rebasant `claude/hello-e87dpo` sur `origin/main` avant d'ouvrir la PR : `git rebase origin/main` a reconnu `034be62` comme déjà appliqué (contenu identique) et l'a sauté tout seul, ne rejouant que le vrai commit de cette session. Pousser en `--force-with-lease` était donc sûr (contenu strictement identique moins le doublon). À refaire si ce cas se represente (branche de travail qui garde un vieux commit déjà mergé ailleurs sous un autre sha).
- **Action utilisateur attendue avant de vérifier le pt 55** : relancer `fetch_collection` (Mon profil) — sans ce passage, le filtre « déjà en collection » reste inactif — puis `scan_recos force=1`.
- Le reste des TODO ci-dessous (pts 37-54) est inchangé : ce sont des vérifications VPS en attente, pas du code à écrire.

Chantier refonte scoring (points 37-43, 5 lots) **mergé en entier sur `main`** (PR #132/#136/#139/#140/#143, + PR #145 ratios de test). État par lot :

- **Lot 1** (Cœur/Aimé, pt 37) : mergé. Pas encore vérifié en conditions réelles VPS.
- **Lot 2** (graphe labels global, pt 38) : mergé, testé VPS à 5000 PUIS 100 000 sorties (pt 40) — deux échelles concluantes. Reste : réimport COMPLET réel VPS (~19M sorties, TODO).
- **Lot 3** (DAG explicite `Ctx`, pt 41) : mergé, pur refactor, rien à vérifier VPS.
- **Lot 4** (précalcul `scorestore.py`, pt 42) : mergé ET vérifié VPS réel (378 208 sorties notées, +112 pistes). **Clos.**
- **Lot 5** (RECOS lit `track_scores`, pt 43) : mergé, **reste à vérifier VPS** (TODO).

## État actuel du projet — résumé

1. **Structure** : `radar_web/` (FastAPI+HTMX, port 8600) ; `crate_jobs.py` (tâches longues via `radar_web/worker.py`) ; `archive/` (Streamlit mort, retiré 2026-09-01, **ne pas toucher**). Nav : Chercher un disque · Mes labels & artistes · Reco Radar · Réglages (« Nouveautés » retirée de la nav — fonctionnalité en pause, cf. pt 56). « 👤 Mon profil » (ex-Mes sources, renommé pt 45) déplacé en haut à droite à côté de la wantlist depuis pt 46.
2. **Déploiement** : push → GitHub (`alixclaudel-hue/radar`) → merge `main` → `.github/workflows/deploy.yml` (VPS OVH : pull + rebuild Docker + health-check, rollback si KO — sauf échec avant `reset --hard`, cf. pt 12). Manuel : `git fetch` avant `reset --hard origin/main` ; `--force-recreate` si conteneur reste "Running" après changement env.
3. **Données** : `/data` VPS (JSON — labels/corpus/graphe/profils/config+token Discogs). Rien dans git. Défaut code `<repo>/data` (identique en local et dans le volume Docker `./data:/data`) — `CRATE_DATA_DIR` n'est plus qu'un override facultatif (cf. pt 44).
4. **Session cloud (celle-ci)** : dépôt frais, sans `/data`/`.env`/token/Playwright/yt-dlp/SSH VPS/mémoire perso. Marche à suivre : éditer, `py_compile`, smoke test, PR. Réseau Trusted = paquets+GitHub seul (Custom + `api.discogs.com`/`bandcamp.com` pour appel réel). Dépannage VPS sans accès = guider l'utilisateur pas à pas, jamais demander identifiants.
5. **Boucle diag VPS en pause depuis 2026-09-06** (jugée non fiable). Trigger `diag-vps` désactivé — ne pas réactiver, ne pas rouvrir issue `Diag <sha>`, ne pas `fire_trigger` dessus sans demande explicite. Contrats gelés hors `.claude/skills/` : `docs/archive/skill-diag.md` + `skill-dev-loop.md`.
6. **Conventions** : avant chaque push, `py_compile` + **`python3 -m unittest discover -s tests`** (suite committée depuis le pt 53, 42 cas) + smoke test ; **jamais `git add -A`** ; commits/commentaires **en français** ; pas de commentaire superflu (le pourquoi, pas le quoi). **Piège CI** : `.github/workflows/ci.yml` démarre un VRAI serveur (`RADAR_NO_AUTH=1`) et teste des routes en HTTP — `unittest` seul ne le couvre pas. Toucher à une route ou à un drapeau de `features.py` oblige à relire cette liste (cf. pt 56, cassée une fois).
7. **Piège Marketplace Discogs** : prix/annonces FR inobtenables (Cloudflare bloque). Garder lien `🇫🇷 voir` + pastille API.
8. **Piège Streamlit** : archivé, mort — jamais y reporter d'évolutions.
9. **Piège Bandcamp** (`bcsearch_public_api`) : endpoint non documenté, repli URL de recherche suffit.
10. **Piège `discogs_get()`** : ne lève jamais d'exception, renvoie `{}` sur échec.
11. **Piège dump Discogs** : `data.discogs.com` sert via `?download=` (query), pas chemin direct — corrigé `dump_url()`/`checksum_url()`.
12. **Piège backup quotidien saturait disque VPS** (`scripts/backup.sh`) : archivait `discogs_dump.sqlite3` (~3G) → 672M→25G en 4j → disque plein → déploiement KO (incident 09/09, PR #77 bloquée). Corrigé PR #78 : dump exclu du `tar` + taille par sous-dossier dans `backup.log`. **Récidive 12/09** : `--exclude` en chemins absolus alors que `tar -C` archive en chemins relatifs → l'exclusion ne matchait jamais rien, `shared/discogs_dump_raw/` (~12G) inclus → backup corrompu 13G. Corrigé : `--exclude` relatifs à `$base`, `discogs_dump_raw/` ajouté à la liste + `.dockerignore` durci (`shared/`).
13. **Piège recherche YouTube** (`ytcache.search_video`) : 1er résultat pouvait être hors-sujet. Corrigé : 15 résultats scorés par métadonnées, cf. pt 25.
14. **Référentiel Discogs local** (`discogs_dump.py`) : index SQLite du dump mensuel, reprenable, bascule atomique. Alimente recherche locale, ranking labels/artistes, graphe co-crédits (mode `taste`).
15. **Entretien de fond** (`RADAR_AUTO_MAINTENANCE=1`) : `canonicalize` hebdo, `profile_labels` hebdo, `build_graph` taste mensuel — automatique.
16. **Le "cerveau"** (`scoring.py`, classe `Ctx`) : `album_score`/`ascore`/`reco_rows` — recalculé à chaque requête (cache mtime).
17. **Jobs** (`crate_jobs.py`) : sous-processus, statut `/data/jobs/*.status.json`, reprenables. Ne pas rebuild le conteneur pendant qu'un job tourne.
18. **Mes retours** (feedback, issue #62) : 🗑 supprime la note ET le commentaire GitHub correspondant.
19. **RECOS RADAR** : playlist **interne** (`recos_playlist.json`), pas une vraie playlist YouTube. Page `/reco-radar`, lecture via API IFrame Player (`cuePlaylist`). Capacité/recherches réglables Réglages (pt 36). Dédoublonnage par IDENTITÉ DE PISTE (artiste+titre) contre tout `recos_history.json` (jamais purgé) — pas juste `video_id`. **Job auto réactivé le 15/09** (cf. pt 47 — `_maybe_recos_scan` avait un `return` sec en tête depuis la pause du 10/09, ignorant `RADAR_RECOS_SCAN=1` déjà présent côté `.env` VPS) : alimentation automatique horaire en plus des boutons manuels 🔄/▶/🗑️🔄. **Renouvellement de la playlist revu (retour utilisateur 14/09)** : plus d'éviction FIFO par ancienneté — une fois `max_tracks` atteint, `job_publish_recos` n'ajoute plus rien (garde-fou avant l'appel réseau YouTube, pour ne pas gâcher le quota) tant qu'une piste n'a pas été retirée. Le chaînage automatique `scan_recos`→`publish_recos` (`_chain_publish_recos`) est supprimé : `publish_recos` ne démarre plus que sur clic explicite de "▶ Alimenter la playlist". Nouveau mécanisme de renouvellement : un clic sur une ligne de la playlist marque la piste "écoutée" (route `/reco-radar/mark-played`) ; le worker (`_maybe_recos_midnight_purge`) purge à minuit heure de Paris toutes les pistes ainsi marquées — ne touche pas `recos_history.json` (dédoublonnage permanent inchangé). Limite connue : un enchaînement automatique du lecteur YouTube entre pistes (fin de piste → suivante, sans clic) ne marque rien comme écouté. Wantlist RADAR (2ᵉ feature) : pas commencée.
20. **Chantier multi-utilisateur** (→ `docs/architecture.md`) : étapes 0-7 faites/déployées. Bloqué étape 4 (HTTPS+domaine), puis 5b (OAuth Discogs/Spotify, 2 apps dev à enregistrer). Reprise 5b : envisager `/model` Opus.
21. **Vendeurs — en pause depuis le pt 56** : le scan vendeurs manuel + `RADAR_SELLER_SCAN=1` hebdo est sans objet tant que `features.SELLERS_ENABLED` vaut `False`.
22. **TODO code** : multi-selects genre/style, `<label for>` non reliés (~30 champs), libellés FR Réglages, liens `/disco` reco/recherche, UI revue artistes « approx », suppression track/DJ dans Mes sets. (Pagination artistes + composant `.tbl` : déjà faits.)
23. **Piège `step` HTML5 sans `min`** (`settings.html`) : poids par défaut non multiple du pas → champ invalide → formulaire bloqué en silence. Corrigé PR #85 : `step="any"` sur les 7 groupes de poids.
24. **Boutiques vérifiées en vente** (`radar/volumo.py`, PR #86) : 🛍️ affiché seulement si disque confirmé en vente. Volumo seule boutique intégrée (API JSON stable). Écartés (Cloudflare/WAF/fermé) : Beatport, Traxsource, Bleep, 7digital, Junodownload.
25. **Recherche YouTube v3** (`ytcache.search_video`, 10/09) : requête enrichie avec le label, 15 résultats (même coût quota que 5), snippet description scoré, bonus chaîne=artiste (+0.1) / chaîne=label (+0.2), 2ᵉ essai sans label si échec. Repli permissif supprimé quand artiste/titre fournis.
26. **Piège lien artiste↔label sans croisement de style** (`Ctx.artist_label_signal`) : partager un label suivi boostait un artiste même hors-style. Corrigé : `artist_ids_for_labels` renvoie les styles DE LA SORTIE précise ; le signal ne compte que les crédits dont un style est dans `wmap` (crédit gardé si sortie sans style renseigné, cf. diagnostic N4).
27. **Piège artiste "Various" pénalisé RECOS** (PR #112) : Discogs crédite "Various" sur les compilations → `_best_match` pénalise (×0.5) car absent des métadonnées vidéo → quasi tout rejeté. Corrigé : `_track_credit_artist()` utilise le crédit RÉEL par piste (`tracklist[].artists`). File déjà scannée avant correctif : régénérer via 🗑️🔄 Forcer.
28. **Piège échec YouTube indistinguable d'un quota épuisé** (`ytcache.search_video`) : `QuotaExhausted` avalée puis mise en cache négatif 7j comme « pas de match » — vidait la file pour rien. Corrigé : (a) `QuotaExhausted` remonte à l'appelant, (b) `NEG_TTL=6h` pour résultats vides (succès restent 7j), (c) `search_video_diag()` journalise la vraie raison. `RECOS_DAILY_SEARCH_BUDGET` 90→45 (un échec coûte 202 unités, pas 100).
29. **Piège suffixes de désambiguïsation Discogs** (« iO (12) ») dans la requête YouTube : retirés pour la RECHERCHE seulement (`_strip_discogs_suffix`) — l'`artist` stocké garde le nom Discogs d'origine (dédoublonnage pt 19 intact).
30. **Piège budget de recherche calé sur les ajouts, pas les recherches** (`job_publish_recos`) : boucle continuait jusqu'à 5 AJOUTS, brûlant tout le quota si échecs (cf. pt 27/28). Corrigé : `RECOS_SEARCHES_PER_RUN` compte les RECHERCHES (succès+échec), pas les ajouts.
31. **Piège bouton "Scanner maintenant" forçait un rescan complet à chaque clic** (`patte_run`) : appel Python direct à `job_launch(job)` sans passer par FastAPI → `bool(Form(""))` vaut **True** → `force=True` permanent sur tout job lancé via `/patte/run/<job>`. Corrigé : `job_launch(job, force="")` explicite.
32. **Piège file RECOS pleine sans explication** (`job_scan_recos`) : scan sauté (file pleine) chaînait `publish_recos`, qui butait sur quota épuisé — message ne le disait pas. Corrigé : `_publish_recos_last_message()` ajoute la vraie raison au message de `scan_recos`.
33. **Piège candidat RECOS détruit au 1er échec YouTube** (`job_publish_recos`) : `continue` sur « vidéo introuvable » ne remettait jamais le candidat en file → perdu définitivement (sortie déjà marquée « vue »). Corrigé : `RECOS_MAX_ATTEMPTS=3`, remis en file entre tentatives.
34. **Piège pt 33 ne recherchait jamais rien** (`ytcache`) : relance rejouait la MÊME clé de cache négatif (`NEG_TTL` pas écoulé) → jamais de vraie 2ᵉ recherche. Corrigé : paramètre `force` sur `search_video`/`search_video_diag`, `job_publish_recos` passe `force=bool(attempts)`.
35. **Piège scan RECOS toujours daté année en cours** (`job_scan_recos`) : requête globale `ORDER BY year DESC LIMIT 5000` — un label prolifique écrasait les autres labels suivis avant même le scoring (même classe que diagnostic D6). Corrigé : requête PAR LABEL (`RECOS_PER_LABEL_LIMIT=500`, depuis supprimée au Lot 5, pt 43).
36. **RECOS — 2 curseurs Réglages** (10/09) : `RECOS_MAX_TRACKS`/`RECOS_SEARCHES_PER_RUN` (constantes dupliquées `app.py`/`crate_jobs.py`) → config `scoring.recos.searches_per_run` (1-45) / `max_tracks` (1-100), réglables `/settings`. Plus besoin de resynchroniser 2 fichiers à la main.

### Refonte scoring — chantier 37-43 (11/09, diagnostic Opus + arbitrages utilisateur)

37. **Lot 1 — labels Cœur(T1)/Aimé(T2)** : `cfg["labels"]`/`["watchlist"]` (2 listes plates) → `cfg["label_categories"]={"1":[...],"2":[...]}` (même modèle qu'`artist_categories`). Migration auto (`store.load_config`) : base→Cœur, watchlist→Aimé dédoublonné. `Ctx.label_tier_map()` remplace tous les accès directs. **Changement de comportement assumé** : `job_scan_veille` couvrait avant SEULEMENT le watchlist — désormais TOUT label suivi (Cœur+Aimé). UI `/univers` : sélecteur Cœur/Aimé + colonne Catégorie, `/reco/label?tier=1|2`, import/export CSV avec catégorie. Route `/univers/labels/import` (dupliquée, morte) supprimée. Nouveaux labels détectés auto → Aimé par défaut. **Mergé PR #132.** Pas encore vérifié en conditions réelles VPS (TODO).
38. **Lot 2 — graphe labels global** (`catalog_labelgraph.py`, nouveau module) : cartographie label↔label sur le référentiel Discogs PARTAGÉ — PAS `labelgraph.py` existant (sous-graphe par-utilisateur, nom distinct tranché avec l'utilisateur). Deux types de lien (colonne `kind`, jamais mélangés) :
    - `"artist"` (non dirigé) : labels partageant un artiste crédité — flux SQL trié, `Counter` flushé tous les 20 000 artistes, UPSERT. Garde-fou `MAX_LABELS_PER_ARTIST=40` (artiste écarté entièrement si dépassé, compte journalisé).
    - `"parent"` (dirigé, a=enfant/b=parent) : hiérarchie Discogs (`<sublabels>`) — nouvelle colonne `labels.parent_id` (id fiable, pas le nom, fragile pour jointure).
    Décisions tranchées avec l'utilisateur avant de coder : "genre" label n'existe pas dans le dump (style existe seulement niveau release, déjà couvert par `label_styles`) ; hiérarchie parent/enfant intégrée comme 2ᵉ type d'arête (pas table séparée).
    Stocké `catalog_labelgraph.sqlite3` séparé, MÊME bascule atomique que `discogs_dump.py`. Job `build_catalog_labelgraph` : no-op si déjà à jour pour le dump courant. Déclencheur événementiel `worker.py` (opt-in `RADAR_CATALOG_LABELGRAPH=1`, check horaire). Infrastructure pure — rien ne consomme encore (prévu lots suivants).
    **Mergé PR #132. Testé VPS à 2 échelles** (détail → pt 40) : 5000 sorties → 1831 labels/3179 liens ; 100 000 sorties → 23 728 labels/153 271 liens. Reste : réimport COMPLET réel (~19M, TODO).
39. **Référentiel élargi TOUS formats** (décision utilisateur, suite pt 38) : `discogs_dump.py` gardait avant SEULEMENT le vinyle 12"/LP — désormais TOUTE sortie gardée, nouvelle colonne `releases.is_vinyl`. Impact : `label_style_counts`/`catalog_labelgraph` portent sur le catalogue complet (pas de filtre — voulu, cf. pt 38). `search_local()`, elle, RE-filtre `is_vinyl=1` depuis pt 45 (elle alimente `/search` et `job_scorestore_releases`→RECOS/wantlist, usages "vinyle à acheter" uniquement). `n_vinyl` change de sens (sous-compte, plus le total inséré). Coût : base SQLite plus grosse (surveiller disque VPS, cf. pt 12). **Mergé PR #132** (même PR que pt 38). **Testé VPS à 2 échelles** : 5000→3568 vinyle/1432 non-vinyle ; 100 000→73 026 vinyle (73,0%). Réimport complet réel : TODO.
40. **Import TEST à durée limitée** (outil de validation jetable, pas une feature — CLI seulement, `VALID_JOBS` de `app.py` inchangé) : `params["limit"]` sur `import_discogs_dump` (`_job_import_discogs_dump_test`) — mêmes fichiers réels, parsing coupé après `limit` éléments/flux, écrit dans `discogs_dump.TEST_DB_PATH` séparé (jamais le référentiel réel). `.gz` conservés (réutilisables pour le prochain run). `build_catalog_labelgraph` symétrique via `params["test"]`, écrit `catalog_labelgraph.TEST_DB_PATH`.
    **3 runs VPS réussis** (historique, gardé pour mémoire) :
    - `limit=5000` (11/09, 4min46s) → 5000 sorties (3568 vinyle)/labels/artistes ; graphe 1831 labels/3179 liens (2258 artiste dont 894 utilisés+1 écarté, 921 parent).
    - `limit=100000` (11/09 22h21-22h27, réutilise les `.gz` déjà en cache — PR #145 ratios déployée juste avant) → 100 000 sorties (73 026 vinyle, 73,0%)/labels/artistes ; graphe 23 728 labels/153 271 liens (138 663 artiste dont 18 986 utilisés+20 écartés=0,1%, 14 608 parent=61,6% des labels — en hausse depuis 50,3% à 5000, cohérent avec le caractère minorant du ratio à petit échantillon). Chiffres vérifiés (recalcul manuel). Référentiel réel jamais touché (confirmé les 2 fois).
    **PR #145** (11/09, mergée+déployée) : messages de fin des 2 jobs test enrichis avec % vinyle / % labels avec parent / % artistes écartés, calculés + protégés division par zéro — utilisés pour les runs ci-dessus.
    **Outil retiré (12/09)** : décision utilisateur de sauter l'étape test intermédiaire (dump trié par ID croissant, échantillon non représentatif des labels suivis) et lancer directement le réimport complet (~19M, tous formats). `_job_import_discogs_dump_test`, `params["limit"]`/`params["test"]`, `discogs_dump.TEST_DB_PATH`, `catalog_labelgraph.TEST_DB_PATH` supprimés du code. **Réimport COMPLET réel : TODO (partie B, VPS).**
41. **Lot 3 — DAG explicite de `Ctx`** : demande initiale "casser un cycle circulaire" — aucun vrai cycle trouvé dans le code (incohérence signalée à l'utilisateur, objectif reformulé : rendre le DAG EXPLICITE, pas corriger un bug inexistant). `NODE_DEPS` (dépendances par nœud) + `topological_order()` vérifié à l'import + `Ctx._memo()` vérifie à l'exécution (erreur explicite si dérive). Effet de bord : `label_artist_signal` promu nœud mémoïsé (ne l'était pas). Aucun changement de comportement (scores identiques avant/après, vérifié). **Mergé PR #136.** Pur refactor — rien à vérifier VPS.
42. **Lot 4 — précalcul scorestore** (nouveau module `scorestore.py`, par utilisateur) : précalcule `Ctx.album_score()` pour chaque sortie des labels suivis puis chaque piste des mieux notées — même formule, aucune nouvelle règle. Pas de bascule atomique (upserts incrémentaux, toujours cohérent entre 2 écritures). 2 tables SQLite WAL (`release_scores`/`track_scores`, `NULL`=non notable≠score bas). 2 jobs chaînés : `scorestore_releases` (purge labels plus suivis, note tout, sans limite de nouveauté) → `scorestore_tracks` (rafraîchit gratuitement les pistes connues + récupère tracklist API pour le top scoré, plafond `fetches_per_run=20`). Config `scoring.scorestore` (`min_score=60`, `fetches_per_run=20`). Worker opt-in `RADAR_SCORESTORE=1`, check 6h fixe. **Mergé PR #139/#140/#141. Vérifié VPS réel** : 378 208 sorties notées, +112 pistes/20 sorties, 0 erreur, chaînage auto OK. **Clos.** Reste non bloquant : confirmer en réel (pas seulement smoke test) que le rafraîchissement gratuit suit bien un changement de goût.
43. **Lot 5 — RECOS lit `track_scores`** (dernier lot, cadré par demande explicite utilisateur) : `job_scan_recos` refondu — lit directement `scorestore.track_scores` JOIN `release_scores` au lieu de rescanner Discogs. Chaque piste porte SON PROPRE score (pas hérité de la release) — nécessite `track_scores.detail_json` par piste (`_migrate_schema` : `ALTER TABLE` idempotent, gère une base déjà peuplée). Conséquence : `job_scan_recos` ne fait PLUS AUCUN appel réseau. `recos_seen.json` obsolète (dédoublonnage par IDENTITÉ DE PISTE suffit, pt 19/29 inchangé). `force` reconstruit la file depuis les scores actuels (plus besoin de vrai rescan réseau). Aucun changement `job_publish_recos`/templates. `RECOS_PER_LABEL_LIMIT` supprimée (morte). **Mergé PR #143. Reste à vérifier VPS (TODO)** : scan sans appel réseau, scores différenciés par piste, feedback 👍/👎 reflète la bonne piste.
44. **Piège `CRATE_DATA_DIR` par défaut sur la racine du repo** (`radar_web/radar/paths.py`, `crate_jobs.py`) : réimport complet du dump (12/09) lancé à la main sur le VPS sans exporter `CRATE_DATA_DIR` → tout écrit dans `<repo>/shared` et `<repo>/jobs` au lieu de `<repo>/data/shared`/`<repo>/data/jobs`, le seul dossier monté par Docker (`./data:/data`). L'appli en prod n'a rien reçu, jobs enchaînés bloqués dans une queue jamais lue par le worker. Cause : `DATA = CRATE_DATA_DIR ou racine du repo` — divergeait silencieusement du volume dès qu'on oubliait la variable. **`crate_jobs.py` dupliquait la même logique en dur** (pas d'import de `paths.py`) : le correctif devait couvrir les deux, pas seulement `paths.py`. Corrigé : défaut `<repo>/data` dans les deux fichiers (`CRATE_DATA_DIR` devient un simple override facultatif, exécution manuelle et conteneur écrivent au même endroit par construction) + `DATA` loggé au démarrage (`crate_jobs.py`, `radar_web/app.py`) pour qu'un futur écart soit visible immédiatement dans les logs.
45. **Retours issue #62 traités (14/09)** : 5 retours utilisateur.
    - Marketplaces `/search` (2 retours 09/09) : déjà résolus par pt 24 (bouton 🛍️ → liste déroulante, Volumo seul si confirmé en vente, rien si aucune boutique) — vérifiés à la relecture du code, rien à corriger.
    - « Mes sources » → **renommé « Mon profil »** partout (nav, titres, renvois inter-pages) — route `/patte` inchangée, contenu inchangé. Décision utilisateur : rester un lien de nav normal (pas de nouvel onglet Mon profil/Wantlist), la wantlist reste l'icône panier en haut à droite. **Précisé pt 57 (17/09)** : ça portait sur la NAV (pas de nouvel onglet dans la barre), pas sur la page de destination — l'icône panier ouvre depuis pt 57 une vraie page `/wantlist` au lieu du 4e onglet caché de « Mes labels & artistes ».
    - `/reco-radar` 🗑️ suppression d'une piste de la playlist : confirmation navigateur retirée (`onsubmit="return confirm(...)"` supprimé du formulaire).
    - Bouton wantlist de `/reco-radar` ajoutait parfois une sortie non-vinyle (CD/digital) : cause racine identifiée — `discogs_dump.search_local()` ne filtrait plus par format depuis l'élargissement pt 39, et alimente à la fois `/search` ET `job_scorestore_releases` (donc les candidats RECOS/le release_id du bouton wantlist). Corrigé à la racine : `search_local()` filtre à nouveau `WHERE r.is_vinyl = 1` (voir pt 39 mis à jour) — corrige le bouton wantlist RECOS et, en prime, le TODO déjà connu de doublons CD/digital sur `/search` (pt 93 ancien TODO). Smoke test hors-ligne (base SQLite synthétique 1 sortie vinyle + 1 CD) : seule la vinyle remonte. **Reste à vérifier VPS réel** (TODO) : `/reco-radar` → bouton wantlist n'ajoute que du vinyle ; `/search` ne renvoie plus de CD/digital.
46. **Retours issue #62 traités (14/09, suite)** : 3 nouveaux retours utilisateur.
    - `/settings` : capacité max playlist RECOS relevée 100→300 (`recos__max_tracks`, `settings.html`) — libellé du curseur aussi corrigé (mentionnait encore l'éviction FIFO par ancienneté, retirée pt 19 le 14/09 et remplacée par le marquage "écouté"+purge minuit). Le curseur permet désormais d'aller jusqu'à 300 ; la valeur effective reste à régler et sauvegarder par l'utilisateur dans `/settings`.
    - « 👤 Mon profil » déplacé du menu nav principal vers le coin haut-droit (`.topbar-right`, à côté de la wantlist 🛒) — `base.html`/`app.css`. Route `/patte` et contenu inchangés.
    - File de jobs sans priorité (`radar_web/radar/jobs.py`, `worker.py`) : un clic utilisateur (ex. « ▶ Alimenter la playlist ») attendait derrière des jobs de fond (scorestore, scan catalogue…) déjà en file. Ajout d'un champ `priority` (1=interactif par défaut, 0=fond — tous les appels `jobs.launch()` depuis `worker.py` passent `priority=0` explicitement) ; `worker._pick()` sert toujours la priorité la plus haute en file d'abord, round-robin entre utilisateurs ensuite. **Ne fait PAS de vrai parallélisme** : l'exécution reste sérielle à dessein (protection rate-limit Discogs partagé, cf. docstring `worker.py`) — un job déjà `running` n'est jamais préempté, seul l'ORDRE de la file change. Smoke test hors-ligne (`_pick` sur des files synthétiques) : priorité interactive passe devant le fond, round-robin intact à égalité, compat champ `priority` absent (anciennes entrées de file). **Reste à vérifier VPS réel** (TODO) : cliquer sur un job interactif pendant qu'un job de fond est EN FILE (pas en cours) doit le faire passer devant.
47. **RECOS RADAR bloquée à 0/240 — diagnostic VPS avec données/API réelles (15/09)** : playlist jamais alimentée malgré `RADAR_RECOS_SCAN=1` dans le `.env`. Deux causes distinctes, corrigées dans le même lot.
    - **Boucle auto morte** : `_maybe_recos_scan` (`worker.py`) avait un `return` sec en tête (pause du 10/09, cf. pt 19) qui ignorait la variable d'environnement déjà activée. Retiré. **Piège associé, traité obligatoirement dans le même lot** : `RECOS_DAILY_SEARCH_BUDGET=45` ne bornait avant que la LONGUEUR de `recos_candidates.json` (`job_scan_recos`), jamais le nombre réel de recherches YouTube/jour — avec la boucle horaire réactivée seule, 24 ticks × `searches_per_run` (jusqu'à 45) pouvaient dépasser le quota (10 000 unités/j, un échec coûte 202, cf. pt 28) dès la matinée, recréant exactement le symptôme "rien ne charge". Corrigé : compteur persistant par utilisateur (`recos_search_budget.json`, remis à zéro à minuit heure de Paris — même horloge que `_maybe_recos_midnight_purge`) appliqué DANS `job_publish_recos` (pas le worker), donc valable aussi sur un lancement manuel (bouton ▶).
    - **Artiste "Various" par piste (2ᵉ occurrence, le correctif pt 27/PR#112 ne couvrait pas tout)** : vérifié en interrogeant l'API Discogs réelle depuis le VPS — CAS A, Discogs ne fournit AUCUN crédit par piste (ex. release 17828710 « Soul Candi Session 4 » : `tracklist[]` sans clé `artists`), le fallback release est alors "Various" lui-même ; CAS B, une piste précise porte littéralement le crédit "Various" au sein d'une sortie par ailleurs bien créditée (ex. release 16062985 « Year002 », piste 1 = megamix d'1h, pistes 2-8 = vrais artistes). Effet de bord observé : les 5 pistes Soul Candi recevaient toutes le même score (84), l'artiste constant cassant la différenciation par piste du Lot 5 (pt 43). Corrigé : `_is_placeholder_artist()` (Various/Various Artists/Unknown Artist/V-A, insensible casse/espaces — 1712/6/1616 occurrences mesurées en base owner) traite ces valeurs comme une absence de crédit dans `_track_credit_artist`, crédit par piste ET fallback confondus. Décision utilisateur : une piste sans artiste exploitable est ÉCARTÉE des candidats RECOS plutôt que cherchée sur YouTube au titre seul (`_best_match` ne sait pas trancher sans artiste) — `job_scan_recos` filtre `WHERE TRIM(ts.artist) <> ''` en SQL. Nouveau helper `_is_continuous_mix()` (motifs "Continuous (DJ) Mix", "DJ Mix", "Megamix" — 430+83 titres en base owner) : filtré à l'écriture (`job_scorestore_tracks`, plus stocké pour les nouvelles tracklists) et à la sélection (`job_scan_recos`, ignore les megamix déjà en base) — **jamais** dans `scoring.real_tracks()`, partagée avec la page sortie (`app.py`) où l'utilisateur veut voir la piste megamix listée.
    - **Nettoyage des données déjà polluées sans rappel API** : 3334 pistes à artiste placeholder sur 403 381 (base owner) — la passe de rafraîchissement gratuite de `job_scorestore_tracks` (relit déjà chaque piste stockée, coût réseau nul) réapplique désormais `_is_placeholder_artist` et vide l'artiste des lignes polluées en un run normal ; un `scan_recos force=1` ensuite reconstruit la file en excluant les candidats "Various" restants.
    - Diagnostic fait par une autre session Claude (VPS, lecture seule — aucun fichier touché sur le VPS, aucun `.stop` résiduel trouvé) ; correctif codé et vérifié par smoke tests hors-ligne dans cette session cloud (base SQLite synthétique reproduisant CAS A/B + megamix + compteur budget). **Reste à vérifier VPS réel** (TODO) : `scorestore_tracks` puis `scan_recos force=1` → 0 appel réseau, candidats "Various"/megamix disparus, scores différenciés par piste sur une compilation ; boucle horaire → la playlist se remplit sur plusieurs ticks sans jamais dépasser 45 recherches/jour au journal `publish_recos`.
48. **Amendement au diagnostic du pt 47 (VPS, mesures sur dump/base réels, 15/09)** : trois points supplémentaires.
    - **Tracklists dans le dump local** : le XML brut du dump CONTIENT bien les crédits par piste (`<tracklist><track><artists>`) — seul `_parse_release_elem`/le schéma SQLite ne les exploitaient pas (le commentaire de `job_scorestore_tracks` affirmant le contraire était faux). Mesuré sur le VPS : 100% des sorties ont une tracklist, 5,7 pistes/sortie, 26,1% avec crédit par piste ; 47 octets/piste → ~5,2 Go mesurés (7-10 Go extrapolés en réel, 43 Go libres). Ajouté : `discogs_dump.TRACKS_DB_PATH` (`discogs_dump_tracks.sqlite3`), base SÉPARÉE de `discogs_dump.sqlite3` (décision : rollback indépendant du catalogue principal si un souci de parsing tracklist survient, même schéma de séparation que `catalog_labelgraph.sqlite3`) — remplie dans la MÊME passe XML que `import_releases` (jamais un second parsing), même bascule atomique. `job_scorestore_tracks` (Lot 4) lit désormais `discogs_dump.tracks_for_release()` en PRIORITÉ (zéro appel réseau) et ne retombe sur l'API que pour une sortie ABSENTE du dump courant (sortie ajoutée sur Discogs depuis le dernier import mensuel) — `SCORESTORE_FETCHES_PER_RUN` ne borne plus que ce repli API, un nouveau `SCORESTORE_LOCAL_BATCH_PER_RUN=20000` borne le lot local (gratuit mais pas illimité par run). **Reste à vérifier VPS réel** (TODO, partie B ci-dessous) : réimport complet avec tracklists, taille finale de `discogs_dump_tracks.sqlite3`, `job_scorestore_tracks` sans appel API sur les sorties déjà dans le dump.
    - **Filtre vinyle déplacé** (inverse une partie du pt 45 du 14/09, arbitrages utilisateur obtenus) : `search_local()` gagne un paramètre `vinyl_only` (défaut `True`) — `job_scorestore_releases` (donc toute la découverte RECOS RADAR) passe désormais `vinyl_only=False` : exhaustif tous formats à la découverte, conformément à la décision utilisateur (« exhaustif à la découverte, vinyle à l'achat »). `/search` (`app.py:957`) **reste** `vinyl_only=True` par défaut, confirmé par l'utilisateur (cet écran reste "quel disque acheter"). Le filtre vinyle se fait maintenant à l'AJOUT wantlist : `app.py::cart_add` vérifie `discogs_dump.lookup_release(rid)["is_vinyl"]` (nouveau champ ajouté à `lookup_release`) — si la sortie proposée n'est pas vinyle, au lieu d'ajouter tel quel, cherche le(s) pressage(s) vinyle de la même piste (artiste+titre) et renvoie le même fragment picker que `/release_matches` (DJ sets, factorisé dans `_vinyl_matches()` partagé) — y compris son message explicite « Aucun vinyle trouvé... » si aucun n'existe (jamais un ajout silencieux raté). Conséquence d'échelle assumée : `release_scores`/`track_scores` grossissent ~4x (25% du catalogue vinyle vs 100% tous formats des labels suivis).
    - **Backup — récidive du piège pt 12 évitée avant qu'elle n'arrive** : `scripts/backup.sh` n'excluait QUE `discogs_dump.sqlite3*` nommément — ni `catalog_labelgraph.sqlite3` (déjà pas couvert), ni la nouvelle `discogs_dump_tracks.sqlite3` (aurait fait exactement le même incident qu'au 09/09 et 12/09). Corrigé : exclusion généralisée à `shared/*.sqlite3*` (tout index reconstructible depuis le dump, pas juste ceux nommés aujourd'hui). **Reliquats signalés, PAS supprimés** (diagnostic VPS en lecture seule) : `shared/discogs_dump_test.sqlite3` (74 Mo) et `shared/catalog_labelgraph_test.sqlite3` (18 Mo), vestiges de l'outil d'import test retiré au pt 40 — plus de la moitié du poids des backups actuels (152 Mo). **À la charge de l'utilisateur de les supprimer sur le VPS** (aucun accès SSH depuis cette session cloud) s'il le souhaite.
    - Vérifié par smoke tests hors-ligne (dump XML synthétique avec tracklists réelles type "Profound Sounds Vol. 1" du diagnostic — crédit par piste, piste sans crédit, sortie absente du référentiel ; base SQLite synthétique vinyle/CD pour le filtre) : parsing + bascule atomique + `job_scorestore_tracks` sans appel réseau + condition de repli wantlist, tous corrects. **Reste à vérifier VPS réel** (TODO) : les trois points ci-dessus, en conditions réelles.

49. **Gap `_is_placeholder_artist` trouvé par la vérif VPS du pt 47, corrigé en 2 temps (15/09, PAS ENCORE déployé — pt 47 seul est en prod via PR #155)** : une session Claude côté VPS a vérifié le correctif du pt 47 sur le code EN PROD — 4 points bons (boucle horaire relancée, nettoyage des 3 placeholders d'origine confirmé en base, `cart_add` testé avec token réel, exclusion backup confirmée). 1 gap : `_is_placeholder_artist()` ne reconnaissait que `{"various", "various artists", "unknown artist", "v/a"}`, ratant "No Artist" (111 pistes `track_scores`), "Various Artists (N)"/"Unknown Artist (N)" (14+8, suffixe de désambiguïsation Discogs jamais retiré avant comparaison — `_strip_discogs_suffix` existait déjà dans le fichier pour un autre usage), "Unknown" (3).
    - **1er correctif (annulé)** : fusion `_PLACEHOLDER_ARTISTS = ARTIST_STOPWORDS | {"v/a"}` — séduisant (set déjà plus large, même bruit ailleurs dans le fichier) mais FAUX : `ARTIST_STOPWORDS` contient aussi "release"/"progressive classics"/"traxsource", du bruit propre au graphe co-crédits labels/artistes, pas des marqueurs "sans artiste". Non détecté par le smoke test hors-ligne (jeu de cas trop étroit, artiste réel homonyme d'un stopword non testé).
    - **Preuve en conditions réelles AVANT correctif déployé** : la session VPS a lancé `scan_recos force=1` puis `publish_recos` sur le code encore en prod (pas mon 1er correctif — jamais mergé) et mesuré l'impact concret : 13 des 15 recherches YouTube du jour (87% du budget quotidien pt 47) parties sur des candidats "No Artist" d'UNE SEULE sortie non-musicale ("Demo Karty Dźwiękowej Sound Blaster Live!"), 0 match trouvé pour aucune ; une piste "No Artist — Intro" (release 22202665, "H2ouse") a quand même matché et s'est retrouvée dans la playlist LIVE de l'utilisateur avec "No Artist" affiché comme artiste. Vérification croisée de la session VPS sur un faux positif possible : "Release (22) — Walk Away" dans le même journal — confirmé (recherche réelle) que "Release (22)" est un VRAI nom d'artiste Discogs, pas un placeholder → exactement le risque du 1er correctif si déployé tel quel.
    - **2ᵉ correctif (retenu)** : `_PLACEHOLDER_ARTISTS` redevient un set DÉDIÉ, volontairement distinct d'`ARTIST_STOPWORDS` : `{"various", "various artists", "va", "v/a", "unknown artist", "unknown", "no artist"}` — couvre les 137 pistes du diagnostic sans absorber le bruit du graphe co-crédits. `_strip_discogs_suffix` toujours appliqué avant comparaison.
    - Vérifié par smoke test hors-ligne élargi : les 5 formes signalées + "Release (22)"/"Traxsource"/"Progressive Classics" (doivent rester de VRAIS artistes, pas des placeholders) + artiste réel/suffixe légitime "Four Tet (2)"/vide/`None` inchangés.
    - **Reste à vérifier VPS réel** (TODO, une fois déployé — la session VPS prévoit un nouveau passage `scan_recos force=1`) : les 137 pistes du diagnostic disparaissent de `track_scores`/candidats RECOS, ET "Release (22)"/tout artiste homonyme d'un ancien mot d'`ARTIST_STOPWORDS` reste bien candidat. Décision utilisateur en attente : nettoyer manuellement l'entrée "No Artist — Intro" déjà publiée dans la playlist live, ou laisser le marquage "écouté" + purge minuit s'en charger naturellement (pt 19).

50. **Budget de recherches RECOS : 45→80 et horloge recalée sur le Pacifique (16/09, demande utilisateur)** : deux changements dans `crate_jobs.py`.
    - `RECOS_DAILY_SEARCH_BUDGET` passe de 45 à 80. Point d'attention documenté dans le code : au pire des cas (80 recherches toutes en échec, ~202 unités chacune, cf. pt 28) il faudrait ~16 160 unités, soit plus que les 10 000/jour d'une clé YouTube gratuite unique — avec une seule clé, 80 reste théorique certains jours et le vrai plafond redevient le quota Google.
    - **Piège horloge du compteur quotidien** (trouvé en prod à l'usage) : `_recos_searches_used_today`/`_recos_searches_record` dataient le compteur `recos_search_budget.json` sur `Europe/Paris`, alors que le quota YouTube Data API se remet à zéro à minuit heure du PACIFIQUE = 9h à Paris. Conséquence observée : budget applicatif à 45/45 depuis le matin alors que le quota Google était déjà reparti — ~9h de recherches bloquées pour rien chaque jour. Corrigé : nouvelle constante `YT_QUOTA_TZ = ZoneInfo("America/Los_Angeles")`, `PARIS_TZ` supprimée de `crate_jobs.py` (elle n'y servait qu'à ces deux fonctions). **`PARIS_TZ` de `radar_web/worker.py` est INCHANGÉE** : elle date `_maybe_recos_midnight_purge` (purge des pistes marquées écoutées, pt 19), mécanisme distinct qui doit rester sur Paris. Messages de fin de `job_publish_recos` reformulés en conséquence (« reprendra à la remise à zéro du quota YouTube (9h à Paris) »).
    - Curseur `/settings` « Recherches YouTube par alimentation » (`recos__searches_per_run`, `settings.html`) : borne haute 45 → 80, alignée sur le nouveau budget quotidien (elle restait calée sur l'ancienne valeur, cf. pt 36). Aucune borne côté serveur (`app.py::settings_save` accepte tout float) — seul le curseur change.
    - Vérifié par smoke test hors-ligne : cumul du compteur, remise à zéro sur changement de jour Pacifique, et surtout 23h Paris (J-1) et 8h Paris (J) partagent bien la même date Pacifique (donc plus de remise à zéro prématurée dans le créneau 00h-09h), 10h Paris bascule sur le jour suivant ; `worker.PARIS_TZ` toujours sur `Europe/Paris`. **Reste à vérifier VPS réel** (TODO).

51. **RECOS toujours bloqué après le pt 50 : rate limit YouTube confondu avec le quota journalier (16/09, diagnostic session VPS après déploiement PR #157)** : une session Claude côté VPS a confirmé les changements du pt 50 corrects et opérationnels en prod, mais a trouvé une cause distincte qui empêchait toujours `publish_recos` de publier quoi que ce soit alors que le budget (80/jour) était loin d'être atteint (21/80 mesuré).
    - **Cause racine** (`radar_web/radar/ytcache.py`) : `_QUOTA_REASONS` mélangeait `quotaExceeded`/`dailyLimitExceeded` (vrai quota des 10 000 unités/jour, persistant) avec `rateLimitExceeded`/`userRateLimitExceeded` (limite de débit par seconde, transitoire) — les deux levaient `QuotaExhausted`, et `job_publish_recos` abandonnait TOUT le run dès la première occurrence (`quota_hit = True`). Preuve en réel apportée par la session VPS : un appel direct avec la MÊME clé a réussi (HTTP 200) 20 secondes après un rejet « quota », et `/videos` répondait aussi 200 — le quota journalier n'était pas en cause, seulement un pic de débit. Aggravant : le repli `"quota" in r.text.lower()` s'appliquait à tout 403/429 dépourvu de `reason` dans `_QUOTA_REASONS`, sans vérifier qu'aucune AUTRE `reason` exploitable n'était présente.
    - **Corrigé** (`ytcache.py`) : séparation en `_DAILY_QUOTA_REASONS` (lève toujours `QuotaExhausted`, comportement inchangé) et `_RATE_LIMIT_REASONS` (nouvelle exception `RateLimited`, distincte) via `_error_kind()`. Le repli texte "quota" ne s'applique plus que si Google ne renvoie AUCUNE `reason`. Ajout d'un retry avec backoff exponentiel (1s/2s/4s) sur la MÊME clé en cas de rate-limit avant de basculer sur la clé suivante (`_get()`), et d'un throttle minimal entre tous les appels HTTP (`_throttle()`, ~3 req/s) — le fichier n'avait aucun `sleep` avant, et les relances de candidats passent `force=True` (donc en rafale, cf. pt 34), ce qui déclenchait précisément le rate-limit.
    - **Corrigé** (`crate_jobs.py::job_publish_recos`) : un `RateLimited` ne met plus `quota_hit=True` (réservé à `QuotaExhausted`) — le candidat repasse simplement en file et le run continue sur les candidats suivants. `searched` (qui alimente `_recos_searches_record`, donc le budget quotidien pt 50) n'est plus incrémenté pour une recherche rate-limited : elle ne coûte pas d'unité Google (confirmé par la preuve en réel ci-dessus), donc ne doit pas grever le budget du jour.
    - **`radar_web/worker.py::_maybe_recos_scan`** : la session VPS signalait aussi une garde anti-double-lancement défaillante (deux `run publish_recos` à 5s d'intervalle observés en logs) en affirmant qu'un job passé de "en file" à "en cours" disparaissait de `jobs.load_queue()`. **Vérifié FAUX en relisant `worker.py::main()`** : une entrée reste dans la file avec `state="running"` pendant TOUTE la durée de `_run()` (bloquant), retirée seulement après — le test `"publish_recos" in queued_names` couvrait donc déjà un job en cours, pas seulement en file. Le vrai gap trouvé à la relecture : le test ne filtrait pas par utilisateur (`uid`), donc un job d'un AUTRE utilisateur du même nom aurait pu bloquer à tort l'auto-scan de owner (latent, pas la cause du doublon observé). Corrigé a minima : filtre ajouté sur `uid == paths.DEFAULT_UID`. Le doublon réellement observé par la session VPS reste plus probablement un chevauchement de deux processus worker pendant un redéploiement (ancien conteneur pas encore arrêté / nouveau déjà démarré) qu'un bug de cette garde — signalé à la session VPS, pas supposé corrigé ici.
    - Vérifié par smoke tests hors-ligne : rate-limit transitoire qui se rétablit après retries sur la même clé ; rate-limit persistant sur toutes les clés → `RateLimited` (jamais `QuotaExhausted`) ; vrai quota journalier → `QuotaExhausted` inchangé ; repli texte "quota" resserré (une autre `reason` exploitable n'est plus écrasée) ; throttle mesuré entre deux appels rapprochés ; `job_publish_recos` sur 2 candidats (1 rate-limited, 1 OK) → le rate-limited repasse en file, le run continue, le budget ne compte que la recherche réussie ; garde `uid` de `_maybe_recos_scan` (job d'un autre utilisateur ne bloque pas, job de owner bloque). **Reste à vérifier VPS réel** (TODO) : cycle réel après déploiement, confirmer que les rate-limits ponctuels n'abandonnent plus le run et ne décomptent plus le budget, que le vrai quota journalier lève toujours l'abandon attendu, et voir si le doublon de lancement se reproduit (auquel cas creuser côté chevauchement de conteneurs pendant le déploiement, pas ce fichier).

52. **Angle mort symétrique du pt 51 : `reason=rateLimitExceeded` peut aussi être un quota journalier (16/09, 2ᵉ diagnostic VPS le même jour, après déploiement PR #158)** : la session VPS a confirmé la #158 opérationnelle en réel (cycle `done: 10/10`, budget non décompté sur les candidats rate-limited, garde anti-doublon uid effective) mais a trouvé le cas inverse.
    - **Cause** : Google renvoie parfois `reason: "rateLimitExceeded"` pour un quota JOURNALIER précis et distinct des 10 000 unités/jour globales — la métrique "Search Queries per day" — alors que `/videos` répond 200 au même instant avec la même clé. Preuve en réel apportée par la session VPS (appel brut depuis le conteneur) : 429, `reason: "rateLimitExceeded"`, mais `message: "Quota exceeded for quota metric 'Search Queries' and limit 'Search Queries per day'..."`. La `reason` ment ici ; seul le `message` tranche. Conséquence avant correctif : les 10 candidats du cycle repartaient tous en "rate" (retry+backoff 1s/2s/4s × 10 = 76s gaspillées par tick horaire), avec un message trompeur (« nouvelle tentative au prochain lancement ») alors que rien ne repasse avant la remise à zéro à 9h Paris (pt 50).
    - **Corrigé** (`ytcache.py::_error_kind`) : avant de conclure `"rate"` sur une `reason` de `_RATE_LIMIT_REASONS`, vérifie maintenant le `message` de l'erreur (`_error_message()`, nouveau) contre `_DAILY_QUOTA_MESSAGE_MARKERS` — si présent, classé `"daily"` (lève `QuotaExhausted`, comme pt 51) plutôt que `"rate"`. **Marqueur volontairement réduit à la seule fenêtre `"per day"`** (relecture Opus de ma propre PR, voir ci-dessous) : le préfixe `"quota exceeded for quota metric"` initialement retenu est commun à TOUTES les métriques Google, y compris les limites transitoires (`"... and limit 'Queries per minute per user' ..."`) — il aurait fait passer un rate-limit par minute pour un quota journalier, réintroduisant par l'autre bout le bug corrigé au pt 51. Le cas symétrique de la PR #158 (rate-limit SANS mention de quota journalier dans le message, prouvé en réel par un retry réussi 20s plus tard) reste `"rate"` — les deux diagnostics sont réels, pas contradictoires, et le commentaire du code l'explicite pour éviter un 3ᵉ aller-retour. `_get()` ne gaspille pas de backoff sur ce cas : sa boucle de retry ne s'active que si `_error_kind() == "rate"`, donc un `"daily"` déguisé en rate-limit sort immédiatement.
    - **Deux défauts trouvés en relisant mon propre code (passe Opus demandée par l'utilisateur AVANT merge), tous deux corrigés dans la même PR** : (a) le marqueur trop large ci-dessus, démontré par un test sur le message réel d'une limite par minute ; (b) `request()` concluait sur la DERNIÈRE clé essayée — avec 2 clés, une clé seulement limitée en débit (récupérable en secondes) suivie d'une clé épuisée pour la journée levait `QuotaExhausted` et abandonnait tout le run. Corrigé : `any_rate` est testé AVANT le quota journalier (la journée n'est perdue que si AUCUNE clé ne peut plus servir). Sans effet à une seule clé, actif dès l'ajout d'une 2ᵉ — donc pertinent vu la décision utilisateur en attente.
    - Vérifié par smoke tests hors-ligne : le cas réel exact remonté (reason `rateLimitExceeded` + message "Search Queries per day") → `"daily"` → `QuotaExhausted` via `request()` ; le cas symétrique de la #158 (même reason, message sans "per day") → toujours `"rate"` (non-régression) ; limites `"per minute per user"` et `"per 100 seconds per user"` → `"rate"` (défaut (a) ci-dessus) ; clé1 rate-limitée + clé2 épuisée → `RateLimited` et toutes clés épuisées → `QuotaExhausted` (défaut (b)) ; `quotaExceeded` classique inchangé ; `userRateLimitExceeded` + "per day" → `"daily"` aussi ; `_get()` ne fait qu'UN appel HTTP (pas de retry) sur le cas déguisé. Cache négatif et `_best_match` revérifiés par lecture du chemin de code : les deux exceptions de quota remontent avant `cache_put`, donc un échec quota n'est jamais mis en cache comme « aucune vidéo trouvée » (pt 28 préservé). **Reste à vérifier VPS réel** (TODO, la session VPS prévoit un nouveau passage après déploiement) : le journal `publish_recos` doit dire « Quota YouTube (recherche) épuisé » (pas « limite le débit ») sur ce type d'erreur, et le budget doit se comporter comme un `QuotaExhausted` classique.
    - **Hors scope, laissé à l'utilisateur** (signalé par la session VPS, pas tranché ici) : une seule clé YouTube configurée — avec la métrique "Search Queries per day" réellement épuisée le 16/09, une 2ᵉ clé aiderait concrètement, mais c'est une décision utilisateur (config Discogs/Google Cloud), pas un correctif de code. Playlist owner passée de 1 à 0 piste entre 15h12 et 16h40 : `job_publish_recos` n'ajoute jamais, sans lien avec ce correctif — pas creusé.

53. **4 correctifs suite relecture VPS des PR #158/#159 (16/09, session cloud)** : la classification quota/rate-limit de `_error_kind` (pts 51-52) est **confirmée bonne en prod** par la session VPS (sondes réelles, 13 cas de classification passés en revue, 0 erreur) — rien retouché côté `_error_kind`/`_DAILY_QUOTA_MESSAGE_MARKERS`/l'ordre de `request()`. 4 bugs trouvés ailleurs dans `crate_jobs.py::job_publish_recos`/`ytcache.py`.
    - **BUG 1 (le plus important)** : `job_publish_recos` ne rattrapait que `QuotaExhausted`/`RateLimited` — un `requests.RequestException` (timeout, coupure réseau ; `ytcache._get` n'a jamais entouré `requests.get` d'un `try`) ou un `RuntimeError` d'erreur API non classée (`ytcache.request`, prouvé atteignable : clé invalide → `reason=forbidden`) remontait tel quel jusqu'à `main()`, qui appelle `job.finish(error=...)` puis `raise` — les `save_json(RECOS_CANDIDATES_PATH, remaining)`/`_recos_searches_record(searched)` de fin de fonction n'étaient JAMAIS exécutés. Perdait les `attempts` déjà incrémentés dans le run ET le décompte des recherches déjà parties (jusqu'à `searches_per_run`, ~900 unités Google), sur le fichier censé précisément protéger le quota. Corrigé : les 4 `save_json`/`_recos_searches_record` déplacés dans un `finally` autour de la boucle (perte limitée au seul candidat en cours au moment d'une exception réellement anormale) ; `requests.RequestException` rattrapée par candidat comme `RateLimited` (remis en file, run continue) plutôt que de planter tout le run sur un aléa réseau.
    - **BUG 2** : `except ytcache.QuotaExhausted` faisait `searched += 1` avant le `continue` — un rejet 429/403 ne consomme aucune unité de quota Google (même argument que la branche `RateLimited` juste en dessous, qui ne le faisait déjà pas). Preuve mesurée par la session VPS : un run à 0 recherche aboutie (100% quota épuisé) avait quand même enregistré `searched=1`. Effet : +1 faux décompte par tick horaire tant que le quota restait épuisé. Corrigé : incrément retiré, cohérent avec `RateLimited`.
    - **BUG 3** : un `RateLimited` sur UN candidat ne coupait pas le run — chaque candidat suivant rejouait sa propre séquence complète de backoff (`ytcache._get`, 1+2+4s sur la même clé) contre une clé déjà connue limitée dans CE run. Mesuré par mock (session VPS) : jusqu'à 40 appels rejetés / ~70s perdues sur 10 candidats. Corrigé : nouveau flag `rate_hit`, testé en tête de boucle comme `quota_hit` — arrête proprement les nouvelles recherches dès la 1re occurrence, sans transformer ça en `QuotaExhausted` ni déclarer la journée perdue (le candidat reste en file, retenté au prochain lancement).
    - **BUG 4 (mineur, docstring seulement)** : `ytcache._throttle` (`_last_call_ts`, global de module) prétendait espacer "toutes clés/threads confondus" — vrai dans le worker (sériel), FAUX entre processus : `radar_web/app.py::/yt/first` tourne dans le conteneur web, avec son propre `_last_call_ts`. Corrigé la docstring seulement (garantie par-processus, pas partagée) — pas de verrou fichier ajouté (demanderait `SHARED_DIR` + lock inter-process, jugé disproportionné sans problème mesuré ; à arbitrer si ça devient un souci en pratique).
    - **Tests committés pour la première fois** (`tests/`, absents du dépôt jusqu'ici — reproché par la session VPS : la frontière quota/rate a basculé 2 fois en 24h sans qu'aucun cas ne soit committé, seulement rejoué à la main) : `tests/test_ytcache_quota.py` (18 tests, `unittest` stdlib, aucune dépendance nouvelle) — les 5 cas prouvés en conditions réelles par la session VPS pour `_error_kind`/`request()` (quotaExceeded classique, rateLimitExceeded+"per day"→daily, rateLimitExceeded nu→rate, "per minute per user"→rate, 429 sans reason+"quota" dans le texte→daily) + non-régression des deux sens du bug + ordre `any_rate`-avant-`daily` de `request()` sur 2 clés. `tests/test_job_publish_recos.py` (4 tests) : BUG 1/2/3 ci-dessus, chemins de fichiers patchés vers un `tempfile.TemporaryDirectory()` (jamais `/data` réel), `ytcache.search_video_diag` simulée — aucun accès réseau/token.
    - Vérifié par `py_compile` + `python3 -m unittest discover -s tests` (22/22 OK). **Reste à vérifier VPS réel** (TODO) : un run `publish_recos` avec un aléa réseau/une erreur API non classée en cours de route ne doit plus vider `recos_candidates.json`/`recos_search_budget.json` ; le budget ne doit plus jamais compter une recherche quota-épuisée ; un rate-limit sur le 1er candidat d'un cycle de plusieurs doit couper net (pas de backoff répété par candidat, journal `publish_recos` visiblement plus court).

54. **Mon profil (`/patte`) mettait 135s à charger à froid, diagnostic VPS 17/09 (sujet indépendant de RECOS)** : `/` redirige sur `/patte`, donc c'est la page d'entrée de toute l'appli. Mesuré côté VPS : `Ctx()` 2,94s + `.stats()` 132,04s. Cause : `stats()["artists_identified"]` fait `len(self.ascore)`, qui force `ascore` → `graph_rescore()` sur TOUT `producer_graph.json` (153 182 artistes, 623 747 co-occurrences, 32 379 label_edges mesurés en prod) pour n'en garder que la longueur — affichée dans UNE tuile de `patte.html` (« artistes croisés dans ton écoute »).
    - **1er correctif (depuis supplanté, voir bullet suivant)** : `Ctx._identified_artist_keys()`, mathématiquement équivalent à `set(ascore)` (`set(tiers) | set(graph_rescore) == set(tiers) | set(edges bruts)`, la partie coûteuse de `graph_rescore` — score, `why`, tri — n'est jamais nécessaire pour un simple compte), tuile inchangée à l'écran.
    - **Décision utilisateur le même jour : les 2 tuiles sont supprimées** (« artistes croisés dans ton écoute » ET « liens entre artistes », `patte.html`). Leurs compteurs `artists_identified`/`graph_edges` sortent de `Ctx.stats()` faute de consommateur, et `_identified_artist_keys()` — qui n'existait que pour alimenter la 1re tuile sans payer `graph_rescore` — devient mort et est supprimé. **`stats()` ne touche donc plus du tout le graphe**, ce qui atteint le but du pt 54 plus directement que le helper.
    - **Portée limitée aux vignettes, arbitrée avec l'utilisateur avant de coder** (`AskUserQuestion`, 3 options chiffrées) : la demande initiale disait « supprime le job associé », mais le seul vrai job derrière ces tuiles est `build_graph`, qui alimente AUSSI la page Reco artistes (elle n'est QUE du graphe), le bloc « labels issus du graphe » de Reco labels, le constructeur/les visualisations de graphe artistes, le terme `graph` (poids 0,14) du score artiste et la moitié graphe de `label_db_signal`. Le supprimer aurait vidé ces features. **Arbitrage utilisateur : vignettes seulement — `build_graph` et tout ce qu'il alimente restent intacts.**
    - **Corrigé aussi** (piège signalé par la session VPS) : `Ctx._key` incluait la mtime du fichier de config ENTIER — enregistrer un réglage sans rapport (ex. une clé API YouTube dans Mon profil) jetait tout `_DERIVED`, y compris le graphe. `_config_scoring_sig()` n'empreinte plus que `scoring`/`artist_categories`/`label_categories`/`taste_categories` — les seuls sous-arbres réellement lus par les nœuds mémoïsés du DAG (Lot 3), confirmé par grep exhaustif de tous les `self.cfg.` de `scoring.py`.
    - **Point du rapport VPS écarté après vérification** (incohérence détectée, pas appliqué tel quel) : la session VPS affirmait que la mtime de `dd.DB_PATH` (référentiel Discogs) « bouge en permanence » pendant un `import_discogs_dump`, invalidant tout à chaque requête. Vérifié FAUX en lisant `discogs_dump.py::finalize_new_db` : la bascule est un `os.replace()` UNIQUE en fin d'import (construction dans un `.new` séparé, cf. pt 14 « bascule atomique ») — une seule invalidation légitime à la fin de l'import, pas un problème pendant. Rien changé sur ce point.
    - Docstring de `Ctx` corrigée (annonçait « fichiers < 3 Mo », faux d'un facteur 27 : `producer_graph.json` mesuré à 69,3 Mo en prod).
    - **Non traité, proposé à l'utilisateur plutôt que décidé seul** (2ᵉ fix demandé par la session VPS) : précalculer `ascore`/`graph_rescore` côté worker (même motif que `scorestore.py`, `_maybe_scorestore_build`), pour que les autres pages qui lisent `Ctx.ascore` directement (`app.py:1717/1927/1989` — Mon univers, recos) n'en paient plus jamais le coût non plus. Le fix ci-dessus suffit à débloquer `/patte`, donc pas fait dans l'urgence ; à arbitrer si ces autres pages restent lentes en pratique.
    - Vérifié par `py_compile` + `tests/test_ctx_stats.py` (5 tests, `Ctx` réel instancié sur données synthétiques isolées de `/data`, pas juste des mocks) : **après `stats()`, ni `graph_rescore` ni `ascore` n'apparaissent dans le cache `_DERIVED`** (garde-fou direct des 135s, par inspection du cache et non par chronomètre — instable en CI), les 2 compteurs retirés ne réapparaissent pas, `_key` stable sur un réglage hors scope, `_key` invalidé sur `artist_categories`/`scoring`. **Reste à vérifier VPS réel** (TODO) : `/patte` s'affiche en quelques secondes à froid après déploiement (au lieu de 135s) et les 2 vignettes ont bien disparu du bloc « En bref » ; enregistrer un réglage sans rapport dans Mon profil (ex. clé YouTube) n'entraîne plus de rechargement lent de la page suivante ; Reco artistes / Reco labels / graphes d'artistes restent fonctionnels (rien n'a été retiré côté `build_graph`).
    - **Correction (diagnostic VPS du même jour sur `/univers?tab=cart`, cf. pt 57)** : l'attribution ci-dessus des 132s à `graph_rescore()` est **fausse**. Mesuré en prod : `graph_rescore()` seul = 2,23s sur 153 182 artistes/32 379 `label_edges` — négligeable. Le nœud réellement coûteux dans la chaîne `stats()` → `len(self.ascore)` était `ascore` → `artist_label_signal` → `discogs_dump.artist_ids_for_labels()` (deux `GROUP BY` sur les 19,4M lignes de `releases`/38M de `release_artists`), pas le graphe producteur. Le correctif appliqué (suppression des 2 tuiles, `stats()` ne touchant plus `ascore` du tout) reste valide et couvrait déjà les deux — seule l'explication écrite ici était incorrecte, à ne pas reproduire dans un futur diagnostic de perf sur `Ctx`.

55. **Playlist RECOS : colonne « Track », vinyle introuvable, disques déjà possédés (17/09, 3 retours utilisateur)** :
    - **Colonne 1 fusionnée** (`reco_radar.html`) : les colonnes « Artiste » et « Piste » deviennent une seule colonne « Track » (`artiste - piste`). Les colonnes Release/Label/Année/Score et les boutons sont inchangés.
    - **Incohérence signalée, PAS un bug de données** : l'utilisateur signalait que la ligne « Inland Knights — 12 Till 8 » affichait Release = « Drop Music » et Label = « USM Records » au lieu de « Big Audio Spidermite » / « Drop Music » (catalogue DRM011). Vérifié **en conditions réelles** contre l'API Discogs depuis cette session : la release stockée est `1228717` = « Drop Music », compilation CD mixée de Craig De Sousa **sur le label USM Records** (catno CDUSM 1375, 2005), dont la piste 2 est bien « 12 Till 8 » d'Inland Knights. Les deux colonnes affichaient donc la vérité Discogs — la piste a simplement été découverte via la compilation CD, pas via le 12" d'origine (`16036`, Drop Music DRM011, 2000). Rien changé au stockage : ni `release_scores.title`, ni `release_scores.label`, ni le parsing du dump (`_parse_release_elem`, relu, correct).
    - **Vrai bug derrière le symptôme** (« la recherche de vinyle ne trouve rien ») : le bouton wantlist de `/reco-radar` postait à `/cart/add` le titre de la SORTIE (`t.release_title`), et `cart_add` le passait tel quel à `_vinyl_matches()` — or `discogs.search(track=…)` filtre sur le champ **track**. Chercher artiste="Inland Knights" + track="Drop Music" renvoie **0 résultat** (mesuré en réel), là où track="12 Till 8" renvoie 3 sorties dont 2 vinyles, y compris le 12" DRM011 recherché. Corrigé : nouveau champ de formulaire `track` (titre de la piste) dans `reco_radar.html`, `cart_add(track=…)` l'utilise en PRIORITÉ pour le repli vinyle, avec repli sur `title` si absent (`/search`, `/release_matches` et les autres écrans postent sans `track` : comportement inchangé). Le picker de pressages vinyle (pt 48) est donc enfin alimenté avec la bonne requête.
    - **Ne plus proposer en reco une piste d'un album déjà possédé** (cas signalé : toutes les pistes d'« Asakusa Light » de Soichi Terada, album en collection Discogs). `collection_cache.json` ne stockait que des COMPTEURS agrégés (labels/artistes), aucune identité de disque : `job_fetch_collection` mémorise désormais `owned_release_ids` + `owned_release_keys` (`_release_identity_key` = `artiste||titre` normalisés, suffixe de désambiguïsation retiré de l'artiste seulement — un titre peut légitimement finir par « (2) »), **collection seulement, jamais la wantlist** (un disque voulu doit rester proposable). `job_scan_recos` écarte les pistes dont la sortie est possédée, par id ET par identité (un autre pressage du même album porte un id Discogs différent, l'id seul ne suffit pas) et compte les exclusions dans son message de fin. Limite connue et assumée : une piste possédée sur un autre disque (compilation, réédition à titre différent) n'est pas reconnue — filtre au niveau ALBUM, pas au niveau piste.
    - **Prérequis opérationnel** : le filtre reste sans effet tant que `fetch_collection` n'a pas été relancé au moins une fois (un `collection_cache.json` écrit avant ce correctif n'a ni ids ni clés — testé, inoffensif). Les pistes DÉJÀ dans la playlist ne sont pas rétro-nettoyées : les retirer (🗑️) ou les laisser passer par le marquage « écouté » + purge minuit (pt 19).
    - Vérifié par `py_compile` + `python3 -m unittest discover -s tests` (36 tests, 14 nouveaux) : base scorestore SQLite synthétique (album possédé par id, même album sous un autre id, album non possédé, cache d'avant le correctif) ; `job_fetch_collection` avec API Discogs simulée (collection mémorisée, wantlist ignorée) ; `/cart/add` via `fastapi.testclient` (titre de piste prioritaire, repli sur le titre de sortie). Les deux points « en conditions réelles » ci-dessus (release 1228717 / recherche vinyle) sont des mesures API Discogs réelles, pas des smoke tests. **Mergé PR #162, déployé en prod le 17/09 (`751d9ed`). Reste à vérifier VPS réel** (TODO).

56. **Deux fonctionnalités mises en PAUSE (17/09, décisions utilisateur — mergé PR #163, déployé en prod `ead6964`)** : « Nouveautés » (/veille) et « Vendeurs » (catalogue partagé + regroupement de la wantlist par vendeur). Interrupteurs dans un module dédié **`radar_web/radar/features.py`** (`VEILLE_ENABLED`, `SELLERS_ENABLED`), **pas dans `app.py`** : `worker.py` tourne dans son propre processus et n'importe jamais `app.py` — sans source commune, une fonctionnalité coupée côté web resterait enfilée par le worker. Exposés aux gabarits via `templates.env.globals`.

    **Nouveautés (/veille)** :
    - **Masqué** : entrée de nav « 📻 Nouveautés » (`base.html`) et tuile « nouveautés / vendeurs » de Mon profil (`patte.html` — ses 2 compteurs, `veille_rules_active`/`sellers`, ne portent QUE sur cette page, plus rien ne les atteignait).
    - **404** (`_veille_guard()`) : `/veille`, `/veille/rules`, `/sellers/add`, `/sellers/remove`, `/inbox/{kind}` (+ `/clear`, `/dismiss`) et `/inbox/meta`. Attention au partage de code : `/inbox/meta` et `/release/meta` (Recherche, TOUJOURS actif) ont un corps quasi identique — la garde est ancrée sur la bonne fonction, vérifié par test.
    - **Jobs à l'arrêt** : `scan_veille`/`scan_sellers` retirés de `VALID_JOBS`, ce qui couvre `/jobs/<nom>/launch` ET `/patte/run/<nom>` (même validation, cf. pt 31). **Ces deux jobs n'ont jamais eu de boucle automatique côté worker** (vérifié : `worker.py` n'enfile que `scan_catalog`, `import_discogs_dump`, `build_catalog_labelgraph`, `scorestore_releases`, l'entretien de fond et les jobs RECOS) — rien d'autre à couper. **`scan_catalog` (Réglages → Catalogue de vendeurs, boucle hebdo `RADAR_SELLER_SCAN=1`) est un job DIFFÉRENT et reste actif** : il alimente l'affinité vendeurs, pas la page Nouveautés.
    - **Rien n'est effacé** : `pages/veille.html`, `partials/inbox.html`, les routes, `job_scan_veille`/`job_scan_sellers` (toujours dans `JOBS` de `crate_jobs.py`, donc lançables en CLI), les règles `cfg["veille_rules"]`, la liste `cfg["sellers"]` et les files déjà scannées restent en place.

    **Vendeurs** (décision du même jour, après la question « à quoi sert `RADAR_SELLER_SCAN` si je n'ai plus de Nouveautés ? » — réponse : à autre chose, d'où cette 2ᵉ pause explicite) : le classement de la wantlist par vendeur se fait mieux directement sur Discogs.
    - **Masqué** : bloc « Regrouper chez un vendeur » de la wantlist (`univers.html`, onglet `cart` — la phrase d'intro sur les frais de port suit le même drapeau) et section « Catalogue de vendeurs » de `/settings`. **Déplacé pt 57 (17/09)** : ce bloc vit maintenant dans `pages/wantlist.html` (nouvelle route dédiée), même drapeau `sellers_enabled` inchangé.
    - **404** (`_sellers_guard()`) : `/cart/sellers`, `/sellers/catalog`, `/sellers/catalog/toggle`, `/sellers/catalog/add`.
    - **Job `scan_catalog` à l'arrêt des DEUX côtés** : retiré de `VALID_JOBS` (bouton Réglages) **et** `worker._maybe_weekly_scan` sort avant de lire `RADAR_SELLER_SCAN` — indispensable, le worker enfile par `jobs.launch()` sans passer par `VALID_JOBS`, la variable d'environnement seule ne suffisait donc pas à couper la boucle hebdo. Plus besoin de toucher au `.env`/compose du VPS.
    - **Conséquence assumée** : `sellers.cart_coverage`, l'index inversé et la note d'affinité vendeurs ne sont plus alimentés ni lus. Le catalogue (`shared/`) et `cfg["sellers"]` restent sur disque, ils vieillissent simplement. **Le TODO opérationnel « scan vendeurs manuel ~1h/141 vendeurs puis `RADAR_SELLER_SCAN=1` » devient donc sans objet** tant que la pause dure.

    **Commun aux deux** : réactivation = drapeau à `True` dans `radar/features.py` + redémarrage appli ET worker (les drapeaux sont lus à l'import pour `VALID_JOBS` et les globaux Jinja, à l'appel pour les routes et la boucle worker).
    - Vérifié par `py_compile` + `tests/test_features_paused.py` (6 tests via `fastapi.testclient` : 9 routes Nouveautés + 4 routes Vendeurs en 404, `scan_veille`/`scan_sellers`/`scan_catalog` hors `VALID_JOBS` alors que ceux des autres pages y restent, aucune trace de « Nouveautés »/« Catalogue de vendeurs » dans le HTML rendu pendant que le reste de la nav et « Référentiel Discogs local » subsistent, **boucle hebdo du worker qui ne lance rien même avec `RADAR_SELLER_SCAN=1`**, gardes qui laissent repasser les drapeaux à `True`). Vérifié aussi hors test : `/univers?tab=cart` répond 200 avec la wantlist et sans le bloc vendeurs. Suite complète : 42 tests OK.
    - **Piège trouvé par la CI, pas par les tests locaux** : `.github/workflows/ci.yml` fait un smoke test HTTP direct sur le serveur démarré (`check /veille '200|303'`), jamais rejoué en local avant push — la CI de la PR #163 a échoué dessus (`/veille` renvoie 404 par design). Corrigé : `/veille` attend désormais `404` explicitement (commentaire renvoyant à ce point) ; `/reco-radar` et `/univers?tab=cart` ajoutés à la liste (routes actives jamais couvertes jusqu'ici). Reproduit puis revérifié en local dans les MÊMES conditions que la CI (`RADAR_NO_AUTH=1`, `CRATE_DATA_DIR` frais, serveur réellement démarré) avant de repousser — pas seulement `unittest`.

57. **Wantlist ~190s à froid + route dédiée (17/09, diagnostic session VPS distinct de RECOS, brief validé par l'utilisateur)** : la session VPS a mesuré `/univers?tab=cart` à ~148-220s à froid sur données réelles (conteneur `radar-web`, HEAD `683c8cf`) — sans lien avec `/patte`/pt 54 (autre page, autre nœud coûteux, cf. correction ajoutée au pt 54 ci-dessus). Brief découpé en 3 volets, seuls A+B traités ici ; **C (tri de la base de 10 187 labels) volontairement PAS codé** (cf. note en fin de point).
    - **Cause (volet A)** : `univers_page()` (`app.py`) calculait `top_aff`/`top_reco` — les raccourcis « mes + affinité »/« mes + recommandations » du constructeur de graphe de labels, utilisés SEULEMENT sur l'onglet `labels` (`univers.html`) — quel que soit l'onglet demandé. `_pick_base_labels` → `_base_labels_ranked` → `c.reco_index` → `ascore` → `artist_label_signal` → `discogs_dump.artist_ids_for_labels()` : deux `GROUP BY` sur `releases`(19,4M lignes)/`release_artists`(38M)/`release_styles`(29,5M), 181s mesurés en prod sur 10 187 labels — payés en entier pour afficher l'onglet wantlist, qui n'en a aucun usage.
    - **Corrigé (A-1)** : `top_aff`/`top_reco` ne sont calculés que si `tab == "labels"` — les autres onglets (`cart` avant sa suppression, `artists`, `sets`) ne déclenchent plus cette chaîne du tout.
    - **Corrigé (A-2, présenté comme ESSENTIEL par la session VPS — prérequis dur du volet C)** : `_base_labels_ranked(c, need_reco=True|False)` : `c.reco_index` n'est plus calculé que si l'appelant trie/affiche RÉELLEMENT par reco (`sort=="reco"` sur `/univers/labels/table`, `metric=="reco"` sur `_pick_base_labels`) — jamais pour un tri par affinité/possédés, ni pour la recherche de nom (`/search/labels`). `_reco` vaut `None` (pas `0`, pour ne pas confondre "pas calculé cette fois" et "reco nulle") dans les lignes où il n'a pas été demandé ; affiché « — » côté template (`labels_table.html`). **Effet de bord assumé** : dans la liste par défaut de `/univers/labels/table` (sans tri explicite), le départage entre labels de même affinité (médiane à 79, plancher à 60 — beaucoup de labels à égalité, cf. volet C) se fait désormais par ordre alpha au lieu du score reco — accepté comme contrepartie du gain de perf, la colonne Reco reste triable au clic pour qui veut ce classement précis.
    - **Non traité (A-3/A-4/A-5, "si le lot reste lisible")** : rendre les 2 boutons raccourcis paresseux (calcul au clic plutôt qu'au chargement), précalculer `artist_label_signal` côté worker (modèle `scorestore.py`), chargement paresseux de `producer_graph.json` dans `Ctx.__init__`. Laissés de côté pour garder cette PR ciblée et relisible — l'onglet labels lui-même reste donc encore lent aujourd'hui (même coût qu'avant, juste plus circonscrit) ; à reprendre si ça reste gênant en pratique.
    - **Volet B (décision utilisateur)** : la wantlist devient une vraie page `/wantlist` — avant, `base.html` pointait l'icône panier vers `/univers?tab=cart`, un 4e onglet caché de « Mes labels & artistes » (`<title>`/`<h1>` "Mes labels & artistes", sous-nav à 3 onglets sans onglet actif, panneau "⚠️ À vérifier" hérité — tout ça avant d'atteindre la wantlist). Nouvelle route `GET /wantlist` (`app.py`) + gabarit `pages/wantlist.html` (extrait tel quel du bloc `{% elif tab == 'cart' %}` d'`univers.html`, garde `sellers_enabled` inchangée, cf. pt 56) — **sans `Ctx()` du tout** : `render()` fournit déjà `n_cart` (lecture `cart.json`, pas de gros fichier) via son `setdefault`, la page n'a besoin de rien d'autre. `base.html:25` pointe désormais sur `/wantlist` ; `/univers?tab=cart` redirige en `303` vers `/wantlist` pour ne pas casser un signet existant. Précise le pt 45 (14/09) : sa décision « pas de nouvel onglet nav » portait sur la barre de navigation, pas sur la page de destination — l'icône panier reste au même endroit (haut à droite), seule sa cible change.
    - **CI** : `.github/workflows/ci.yml` attend désormais `303` explicitement sur `/univers?tab=cart` (plus `200|303` : ce n'est plus une vraie page) et ajoute `/wantlist` à la liste balayée.
    - **Volet C — délibérément PAS fait, nécessite un arbitrage utilisateur** : la session VPS propose un écran de tri de la base de labels Cœur (10 179 labels, jamais triés depuis un import CSV initial) avec filtre/tri par `coverage` (part du catalogue classée dans un style suivi — signal qui discrimine, contrairement à l'affinité : plancher mathématique à 60 par construction de `affinity_score`, cf. diagnostic N4, PAS un bug). Nécessite d'abord ce lot-ci mergé (A-2 est son prérequis dur, sinon l'écran de tri hérite des mêmes 181s). Points à trancher avec l'utilisateur avant de coder, non tranchés ici : seuil de coupe sur `coverage`, mécanique de retrait (suppression définitive / rétrogradation Cœur→Aimé / nouvelle catégorie "écarté"), sort des 1 419 labels `aff=None` (indécidables, distincts des 1 461 `coverage=0%` — ne pas les traiter pareil).
    - Vérifié par `py_compile` + `python3 -m unittest discover -s tests` (51 tests, 9 nouveaux dans `tests/test_wantlist_perf.py` : `/univers?tab=cart` redirige sans jamais créer de `Ctx()`, `/wantlist` idem, les onglets `artists`/`sets` ne déclenchent plus `reco_index`/`ascore`/`artist_label_signal`, l'onglet `labels` continue de les déclencher — non-régression —, `_base_labels_ranked`/`_pick_base_labels` avec/sans `need_reco` inspectés via le cache `_DERIVED`, jamais un chronomètre) + smoke test HTTP local dans les mêmes conditions que la CI (`RADAR_NO_AUTH=1`, `CRATE_DATA_DIR` frais, serveur réellement démarré, toutes les routes balayées OK). **Mergé PR #165, déployé en prod le 17/09 (`8dae861`). Reste à vérifier VPS réel** (TODO) : temps de chargement réel de `/wantlist` à froid (visée : ~0,1-quelques s, plus 148-220s) ; `/univers?tab=cart` redirige bien en 303 sans casser un signet existant ; l'onglet `/univers?tab=labels` reste fonctionnel (raccourcis de graphe toujours alimentés) malgré son coût inchangé.

## TODO — prochaine session

- **Pt 57 (perf `/univers?tab=cart` ~190s + route `/wantlist` dédiée, 17/09)** : **PR #165 mergée et déployée** (`8dae861`). Reste à vérifier VPS réel :
  - `/wantlist` à froid : quelques dixièmes de seconde au lieu de 148-220s mesurés sur `/univers?tab=cart` ; `n_cart`/liste/synchronisation Discogs identiques à avant.
  - `/univers?tab=cart` (ancien lien, signets existants) redirige bien en 303 vers `/wantlist`.
  - `/univers?tab=labels` reste fonctionnel : raccourcis « mes + affinité »/« mes + recommandations » du constructeur de graphe toujours alimentés (coût inchangé sur cet onglet, volontairement pas optimisé ici — cf. A-3/A-4/A-5 non traités).
  - `/univers/labels/table` : tri par défaut (sans clic sur une colonne) légèrement différent qu'avant à affinité égale (alpha au lieu du départage par reco, cf. pt 57) — vérifier que ça reste acceptable en usage réel ; tri explicite sur la colonne "Reco" (clic) doit toujours donner les vrais scores.
  - **Volet C (tri de la base de 10 187 labels Cœur/Aimé)** : PAS codé, en attente d'arbitrage utilisateur (seuil de coupe sur `coverage`, mécanique de retrait, sort des labels `aff=None`) — à ne PAS commencer avant que ces décisions soient prises, et seulement une fois pt 57 (A-2) en prod.

- **Chantier refonte scoring (pts 37-43) mergé en entier.** Reste à vérifier sur VPS :
  - **Lot 5 (pt 43)** : cliquer 🔄 Scanner maintenant sur `/reco-radar` → confirmer au journal 0 appel réseau (immédiat) + scores différenciés par piste (pas tous identiques sur une même sortie). Puis ▶ Alimenter la playlist → recherche YouTube/ajout inchangés. Feedback 👍/👎 sur une piste de compilation/featuring → `f_label`/`f_artist`/`f_style` doivent refléter CETTE piste, pas la release entière.
  - **Lot 4 (pt 42)** : déjà vérifié VPS réel, clos. Reste non bloquant : confirmer que changer un réglage de goût puis relancer `scorestore_tracks` seul rafraîchit les scores déjà connus SANS nouvel appel API (smoke test seulement pour l'instant).
  - **Lot 1 (pt 37)** : la migration ne perd aucun label (comparer total Cœur+Aimé à l'ancien base+watchlist dédoublonné), ajout/retrait/tier fonctionne dans `/univers`, `job_scan_veille` couvre bien TOUS les labels suivis (pas seulement l'ex-watchlist).
  - **Lots 2/39 + import TOUS formats (pts 38-40)** : testé VPS à 5000 ET 100 000 sorties, concluant les 2 fois. **Reste le réimport COMPLET réel** (~19M sorties, pas un `limit`) : lancer `import_discogs_dump` en entier sur le VPS, confirmer taille finale/temps (vs ~3G/~1h45 vinyle seul — attention disque, cf. pt 12), `n_total`≫19 417 067 avec `n_vinyl` cohérent en sous-compte. Puis activer `RADAR_CATALOG_LABELGRAPH=1`, lancer `build_catalog_labelgraph` une fois à la main sur le référentiel complet, confirmer temps d'exécution + ratios plausibles à cette échelle + `MAX_LABELS_PER_ARTIST=40` toujours pas un goulot.
  - **Lot 3 (pt 41)** : pur refactor, rien à vérifier.
  - **Pt 45 (filtre vinyle)** : SUPERSEDÉ en partie par le pt 48 — `/search` reste vinyle seul (confirmé), mais `job_scorestore_releases` est repassé tous formats (`vinyl_only=False`, découverte exhaustive). Vérifier VPS réel : `/search` ne renvoie toujours pas de CD/digital ; bouton wantlist (RECOS ou ailleurs) sur une sortie non-vinyle propose bien le picker de pressages vinyle au lieu d'ajouter tel quel, avec message explicite si aucun n'existe.

- **Pt 47 (diagnostic VPS 15/09 — playlist bloquée à 0/240, boucle auto réactivée + artiste "Various"/megamix)** : reste à vérifier VPS réel une fois la PR mergée/déployée (une autre session Claude, celle qui a fait le diagnostic, prévoit de relancer `scorestore_tracks` puis `scan_recos force=1` côté VPS et de confirmer à l'utilisateur) —
  - `scorestore_tracks` : la passe de rafraîchissement gratuite nettoie bien les artistes placeholder déjà stockés (comparer un échantillon avant/après) ; `scan_recos force=1` : 0 appel réseau au journal, aucun candidat "Various"/megamix, scores différenciés entre pistes d'une même compilation.
  - Boucle horaire (`RADAR_RECOS_SCAN=1`) : la playlist se remplit sur plusieurs ticks sans jamais dépasser 45 recherches/jour (vérifier `recos_search_budget.json` dans `/data/users/<uid>/`) ; un lancement manuel (▶) respecte le même plafond quotidien que la boucle.

- **Pt 49 (gap `_is_placeholder_artist` trouvé par la vérif VPS du pt 47, 2ᵉ correctif après annulation du 1er)** : reste à vérifier VPS réel une fois déployé — nouveau passage `scorestore_tracks` puis `scan_recos force=1`, confirmer (a) que les 137 pistes "No Artist"/"Various Artists (N)"/"Unknown Artist (N)"/"Unknown" relevées par le diagnostic ont disparu de `track_scores`/des candidats RECOS, ET (b) qu'un artiste réel homonyme d'un ancien mot d'`ARTIST_STOPWORDS` (ex. "Release (22)", confirmé réel par la session VPS) reste bien candidat, pas écarté à tort. Décider (utilisateur) : nettoyer manuellement l'entrée "No Artist — Intro" déjà dans la playlist live (release 22202665, "H2ouse"), ou laisser le marquage "écouté"+purge minuit s'en charger.

- **Pt 48 (amendement 15/09 — tracklists dans le dump, filtre vinyle déplacé, backup)** : reste à vérifier VPS réel —
  - **Réimport complet AVEC tracklists** (partie B, déjà en TODO au pt 40 pour le reste) : confirmer taille finale de `discogs_dump_tracks.sqlite3` (mesuré ~5,2 Go sur échantillon, extrapolé 7-10 Go réel — surveiller le disque VPS pendant le rebuild, cf. pt 12) et temps d'import (le parsing tracklist ajoute du travail CPU dans la même passe XML, pas de nouveau téléchargement).
  - `job_scorestore_tracks` sur le référentiel complet : confirmer au journal que les sorties couvertes par le dump ne déclenchent AUCUN appel API (compteur "via le dump local" ≫ "via l'API" dans le message de fin), et que le repli API ne se déclenche que pour des sorties réellement absentes du dernier import mensuel.
  - Bouton wantlist (`/cart/add`) sur une sortie non-vinyle issue de RECOS : confirmer que le picker de pressages vinyle s'affiche (pas un ajout CD/digital silencieux), et le message explicite quand aucun pressage vinyle n'existe pour la piste.
  - `job_scorestore_releases` tous formats : confirmer l'ordre de grandeur ~4x sur `release_scores`/`track_scores` (pas de dérive disque/temps de job anormale).
  - Backup : vérifier qu'un prochain backup n'inclut plus `discogs_dump_tracks.sqlite3` (taille du `.tgz.enc` cohérente, pas de saut brutal). Décider (utilisateur) : supprimer ou garder `shared/discogs_dump_test.sqlite3`/`catalog_labelgraph_test.sqlite3` (reliquats signalés, jamais supprimés faute d'accès SSH depuis les sessions cloud).

- **Pt 50 (budget 80 + horloge Pacifique, 16/09)** : vérifier VPS réel après déploiement — le journal `publish_recos` ne se coupe plus avant 9h du matin sur un budget prétendument épuisé ; `recos_search_budget.json` (`/data/users/<uid>/`) change de `date` vers 9h heure de Paris, pas à minuit ; la playlist se remplit jusqu'à 80 recherches/jour tant que le quota Google réel suit (une seule clé = plafond effectif atteint avant 80 si beaucoup d'échecs). Décision utilisateur en attente : ajouter une 2ᵉ clé YouTube pour rendre les 80 réellement atteignables.

- **Pt 51 (rate-limit YouTube confondu avec quota journalier, 16/09)** : vérifier VPS réel après déploiement — le journal `publish_recos` ne doit plus abandonner tout le run sur un simple pic de débit (« YouTube limite le débit » au lieu de « Quota YouTube épuisé »), le compteur `recos_search_budget.json` ne doit plus compter ces recherches rate-limited, et un vrai quota journalier épuisé doit toujours lever l'abandon habituel. Si un doublon `scan_recos`/`publish_recos` se reproduit, vérifier s'il coïncide avec une fenêtre de redéploiement (deux workers actifs en même temps) plutôt que dans `_maybe_recos_scan`, déjà revérifiée correcte pour un job en cours du même utilisateur.

- **Pt 52 (quota journalier déguisé en rate-limit, 16/09)** : vérifier VPS réel après déploiement — un vrai quota "Search Queries per day" épuisé doit relever `QuotaExhausted` (message « Quota YouTube (recherche) épuisé », pas « limite le débit »), sans backoff gaspillé, et le budget quotidien doit refléter cet abandon comme avant la PR #158. Vérifier aussi que le cas transitoire de la #158 (rate-limit sans "per day") continue de fonctionner sans régression.

- **Pt 53 (4 correctifs relecture VPS des PR #158/#159, 16/09)** : vérifier VPS réel après déploiement — un run `publish_recos` interrompu par un aléa réseau/une erreur API non classée ne doit plus vider `recos_candidates.json`/`recos_search_budget.json` (BUG 1) ; le budget quotidien ne doit plus jamais compter une recherche rejetée pour quota épuisé (BUG 2, comparer `recos_search_budget.json` avant/après un run 100% quota-épuisé) ; un rate-limit sur le 1er candidat d'un cycle de plusieurs doit couper le run net, sans backoff répété candidat par candidat (BUG 3, journal `publish_recos` nettement plus court/rapide sur ce cas). `tests/` (nouveau, `python3 -m unittest discover -s tests`) à relancer si `ytcache.py`/`crate_jobs.py::job_publish_recos` évoluent encore sur ce chemin.

- **Pt 54 (Mon profil `/patte` 135s à froid + suppression des 2 vignettes, 17/09)** : vérifier VPS réel après déploiement — première visite de `/patte` après redémarrage/déploiement en quelques secondes (pas 135s) ; les vignettes « artistes croisés dans ton écoute » et « liens entre artistes » ont bien disparu du bloc « En bref », les 6 autres compteurs restent ; enregistrer un réglage sans rapport dans Mon profil (ex. clé API YouTube) ne provoque plus de rechargement lent de la page suivante ; **Reco artistes / Reco labels / graphes d'artistes toujours fonctionnels** (`build_graph` volontairement conservé, cf. arbitrage utilisateur au pt 54). Décision utilisateur en attente (proposée, pas tranchée) : précalculer `ascore`/`graph_rescore` côté worker (comme `scorestore.py`) pour que Mon univers/recos (`app.py:1717/1927/1989`) n'aient plus jamais à payer ce calcul non plus — pas fait, le correctif `/patte` seul lève l'urgence.

- **Pt 55 (playlist RECOS : colonne Track, vinyle introuvable, albums possédés, 17/09)** : vérifier VPS réel après déploiement —
  - `/reco-radar` : colonne unique « Track » (`artiste - piste`), le reste du tableau inchangé.
  - Bouton wantlist sur la ligne « Inland Knights — 12 Till 8 » (compilation CD « Drop Music ») : le picker doit proposer le 12" Drop Music DRM011 (release 16036), au lieu du message « aucun vinyle trouvé ».
  - Relancer `fetch_collection` (Mon profil), vérifier que `collection_cache.json` contient `owned_release_ids`/`owned_release_keys`, puis `scan_recos force=1` : aucune piste d'« Asakusa Light » (Soichi Terada) ni d'un autre album de la collection dans les candidats, et le message de fin compte les exclusions. Vérifier aussi qu'un disque seulement en WANTLIST reste bien proposable.

- **Pt 56 (Nouveautés + Vendeurs en pause, 17/09)** : vérifier VPS réel après déploiement — plus d'entrée « 📻 Nouveautés » dans la nav ni de tuile « nouveautés / vendeurs » sur Mon profil ; `/veille` en 404 ; plus de bloc « Regrouper chez un vendeur » sur `/univers?tab=cart` ni de section « Catalogue de vendeurs » dans `/settings` ; le reste de la nav, la wantlist et les jobs des autres pages intacts ; au journal du worker, plus aucun `scan_catalog hebdo enfilé`. **Quand une fonctionnalité reprendra** : son drapeau à `True` dans `radar_web/radar/features.py` + redémarrage appli ET worker, rien d'autre à défaire.

- **RECOS RADAR — anciens correctifs jamais revérifiés en conditions réelles VPS** (points 27-36, tous du 10/09, `job_publish_recos` inchangé par le Lot 5) : lancer 🔄 Scanner puis ▶ Alimenter plusieurs fois de suite et confirmer au journal :
  - budget = recherches pas ajouts (pt 30), pas de double-scan sur clic répété (pt 31), raison affichée si file pleine (pt 32), candidat retenté 3× avant abandon (pt 33) avec vraie relance réseau à chaque fois (pt 34, pas cache figé), sorties d'années variées pas juste l'année courante (pt 35), curseurs Réglages respectés (pt 36).
  - Cas particuliers à re-tester : compilations "Various" (pt 27, ex. Aquasonic Vol.1, Defected Classics) ; pistes bloquées à 2-3 tentatives avant correctif (pt 33/34, ex. Noir/Haze — Around, Candi Staton — Hallelujah Anyway) ; raison quota vs score trop strict dans le journal `publish_recos` (pt 28-29 — si « quota épuisé » attendre minuit Pacifique, si « meilleur score X < 0.5 » sur une piste évidente c'est `MIN_MATCH_SCORE` qui est à revoir).
  - Si tout passe : le chantier RECOS de la session du 10/09 peut être considéré clos.

- **Ingestion réelle jamais testée depuis la factorisation `_ingest_lookup_loop`** (PR #111, 10/09) : tester une vraie ingestion YouTube (au moins) contre l'API réelle avant de clore le skill `factorize` sur `crate_jobs.py`.

- ~~**TODO opérationnel** : scan vendeurs manuel une fois puis `RADAR_SELLER_SCAN=1`~~ — **sans objet depuis le pt 56** (fonctionnalité Vendeurs en pause ; la boucle hebdo ignore la variable d'environnement). À ressortir seulement si `SELLERS_ENABLED` repasse à `True`.

- **Pt 46 (retours issue #62 du 14/09)** : vérifier VPS réel — priorité de file (déclencher un job de fond via un opt-in `RADAR_*` puis cliquer un job interactif pendant qu'il est encore EN FILE, confirmer au journal `worker` qu'il passe devant) ; curseur capacité playlist RECOS jusqu'à 300 dans `/settings` (sauvegarder puis vérifier que la valeur est bien prise en compte par `job_publish_recos`) ; « Mon profil » bien visible en haut à droite sur mobile (largeur écran étroite, `.topbar-right` avec `flex-wrap`).

- **TODO code résiduel** (pt 22) : multi-selects genre/style, `<label for>` non reliés (~30 champs), libellés FR dans Réglages, liens `/disco` depuis reco/recherche, UI de revue des artistes « approx », suppression par track/DJ dans Mes sets.
