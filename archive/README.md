# Archive

App Streamlit de Radar (`crate_radar.py`), **retirée le 2026-09-01** (étape 7 de
`docs/architecture.md`). Conservée pour référence — non maintenue, non construite.
La logique de notation vit désormais dans `radar_web/radar/scoring.py` ; les jobs
dans `crate_jobs.py` (toujours actif, partagé). Historique complet dans git.
